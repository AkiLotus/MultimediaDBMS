chạy lệnh theo thứ tự sau:
python create_normalize_values (tạo min,max để normalize)
python extract_vector_feature
python build_tree

để query file trong thư mục test_sounds, chạy lệnh:
python query.py ../test_sounds/filename
	ví dụ : python query.py ../test_sounds/birds002.wav
